Algorithms and Pseudocode


Counting backwards that skips the half and breaks at the half of that half

Algorithms:
1. define the variables of the number of iterations, number in which it will start, the divide by two and another divide by two by that devide by two
2. prompt user for input in regards to the variables
3. devide the i into two and devide that halved into another two
4. for loop containing the number of iteration and the number in which it will start
5. inside the for loop, create an if statement that continues to the half of the number in which it will start
6. create another else if statement inside/outside that if statement that breaks into the half of that if statement
7. print statement for result

Pseudocode
int i;
int iteration;
int half = /2

    printf("What is the number in which you would like to start to count backwards? ");
    scanf
    printf("Now what is the number which you would like to end? ");
    scanf

        for(int i; int i >= iteration; i--)
        {
            if(i is half)
            {
                continue;
            }else if(half of that halved)
            {
                break;
            }
            printf("result")

        }


int main()
{
int i;
int iteration;
int halfI;
int halfITwo;


    printf("What is the number in which you would like to start to count backwards? ");
    scanf("%d", &i);
    printf("Now what is the number which you would like to end? ");
    scanf("%d", &iteration);

    halfI = i / 2

        for(int i; int i >= iteration; i--)
        {
            if(i = halfI)
            {
                continue;
            }else if(i = halfITwo)
            {
                break;
            }
            printf("Output: %d", i);
        }
    return 0;
}
























